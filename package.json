{
  "private": true,
  "name": "react-protector-packages",
  "scripts": {
    "pages": "push-dir --dir=website/build --branch=gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lerna": "^2.9.0",
    "push-dir": "^0.4.1"
  }
}
